# 医学问询智能体 · 项目架构与启动说明

> 本文档基于源码逐文件梳理，覆盖「项目如何启动」「有哪些架构分层」「有哪些核心机制」三大部分，可作为新成员上手与后续维护的权威参考。
>
> 代码根目录：`medical-agent/`
> 后端技术栈：FastAPI + Uvicorn + SQLite + cryptography(Fernet) + 自研 BM25 RAG（零外部检索依赖）
> 前端形态：桌面网页版（钉钉风格静态页）+ 微信小程序版（WXML）

---

## 一、项目如何启动（启动机制）

### 1.1 入口与进程模型

| 项 | 说明 |
|---|---|
| 真正的应用对象 | `backend/app/main.py` 中的 `app = FastAPI(...)` |
| 启动命令（脚本） | `uvicorn app.main:app --host 0.0.0.0 --port 8600` |
| 便捷入口 | 在 `backend/` 目录下执行 `python -m app`（见 `backend/app/__main__.py`） |
| 单进程 | 一个 Uvicorn 进程同时承载 **REST API + SSE 流式接口 + 桌面前端静态托管** |
| 前端部署形态 | 后端把 `frontend/desktop` 挂载为 `/static`，并把 `/` 指到 `index.html`（同源部署，天然规避 CORS） |

> 注：`__main__.py` 默认 `host=127.0.0.1`、`port=8600`，由环境变量 `APP_HOST/APP_PORT/APP_RELOAD` 覆盖；而 `scripts/manage.sh` 直接以 `0.0.0.0` 启动。`config.yaml` 里的 `server.host/port` 供配置参考，实际常由脚本/环境变量驱动。

### 1.2 标准启动流程（推荐：统一脚本）

```bash
# 1) 首次：创建虚拟环境并安装依赖
bash scripts/setup.sh            # Windows 也可双击 scripts/setup.bat
#   → python -m venv .venv
#   → pip install -r backend/requirements.txt

# 2) 运行桌面版（启动后端 + 自动打开浏览器）
bash scripts/run_desktop.sh      # 或 scripts/run_desktop.bat
#   → 启动后端（默认 http://localhost:8600）
#   → 打开网页（钉钉排版布局）

# 3) 运行小程序版（启动后端 + 打印导入指引）
bash scripts/run_weapp.sh
#   → 用微信开发者工具导入 weapp/ 目录即可
```

所有快捷脚本（`setup/run_desktop/run_weapp/build_desktop/build_weapp` 的 `.sh` 与 `.bat`）均内部转发到 `scripts/manage.sh <子命令>`，子命令见下表：

| 命令 | 作用 |
|---|---|
| `setup` | 创建 `.venv` 并安装 `backend/requirements.txt` |
| `run-desktop` | 启动后端 + 打开桌面网页（先自检 8600 是否已运行，避免重复启动） |
| `run-weapp` | 启动后端 + 打印小程序导入步骤 |
| `build-desktop` | 拷贝 `frontend/desktop` → `dist/desktop`（纯静态产物，可丢 Nginx） |
| `build-weapp` | 校验小程序源码完整性 → `dist/weapp` |

### 1.3 配置入口 `config.yaml`

启动前需关注的关键配置（已含默认值，留空则自动降级）：

```yaml
server:
  host: "0.0.0.0"
  port: 8600
  cors_origins: ["http://localhost:8600", "http://127.0.0.1:8600"]

security:
  jwt_secret: ""      # 留空 → 首次启动自动生成并写入 data/secret.key
  fernet_key: ""      # 留空 → 同上

llm:
  providers:
    zhipu:    { api_key, base_url, chat_model, vision_model, embedding_model }
    deepseek: { api_key, base_url, chat_model, vision_model, embedding_model }
    openai:   { api_key, base_url, chat_model, vision_model, embedding_model }  # 预留扩展
  default_provider: "zhipu"

rag:
  chunk_size: 500     # 分块字符数
  chunk_overlap: 80   # 相邻块重叠
  top_k: 6            # 每次检索召回片段数
  min_score: 0.05     # 低于该相关度的片段不作为依据
```

### 1.4 启动时的「自举」行为（首次运行）

1. `config.py` 加载 `config.yaml`；若 `jwt_secret`/`fernet_key` 为空，调用 `_ensure_secret()` 生成 32 字节随机密钥，base64 写入 `data/secret.key`（格式 `name=value`，幂等：已存在则复用）。
2. 首次访问受保护接口时，`deps.get_current_user` 会确保该用户数据目录 `data/spaces/{user_id}/` 存在。
3. 首次进行知识库检索时，`indexer.get_index()` 在发现 `kb/index.json.enc` 缺失时会基于已上传文档即时重建索引。

### 1.5 受限模式（未配置任何 API Key）

- 知识库本地 BM25 检索**仍可用**（不依赖大模型）。
- 大模型问答 / 拍照识药 **不可用**：接口返回安全兜底文案（「请配置模型密钥后重试，若情况紧急请直接就医」），**绝不编造**。
- 健康建议在无模型时回退为固定安全建议（规律作息、多饮水等）。

---

## 二、整体架构（分层）

### 2.1 目录结构（核心源码）

```
medical-agent/
├── config.yaml                 # 主配置（API Key、端口、RAG 参数）
├── backend/
│   ├── requirements.txt
│   └── app/
│       ├── main.py             # 入口：FastAPI 实例 + 路由汇总 + 静态托管
│       ├── __main__.py         # python -m app 启动器
│       ├── config.py           # 配置加载 + 密钥自举
│       ├── auth.py             # 账户 + 自签 JWT（SQLite）
│       ├── crypto.py           # 按用户派生密钥的 Fernet 加密 + 密码哈希
│       ├── storage.py          # 按用户隔离的数据空间（加密落盘）
│       ├── prompts.py          # 提示词模板
│       ├── summary.py          # 对话标题自动摘要
│       ├── reports.py          # 报告库存储/归类/检索
│       ├── med_cats.py         # 病情术语 → 系统分类统一映射
│       ├── llm/                # 模型适配层
│       │   ├── __init__.py     # provider 注册表与获取
│       │   └── base.py         # LLMProvider 抽象 + OpenAI 兼容实现
│       ├── rag/                # 检索增强生成
│       │   ├── loader.py       # 文档解析 PDF/DOCX/MD/TXT → 文本
│       │   ├── chunker.py     # 分块（paragraph/fixed/sentence）+ 质量统计
│       │   ├── indexer.py     # BM25 索引与检索（可选语义加权）
│       │   ├── verify.py      # 检索命中率验证
│       │   └── graph.py       # 知识图谱抽取（离线、确定性）
│       └── routers/            # HTTP 接口层
│           ├── deps.py         # get_current_user 鉴权依赖
│           ├── auth_router.py
│           ├── chat_router.py
│           ├── kb_router.py
│           ├── profile_router.py
│           ├── reports_router.py
│           └── vision_router.py
├── frontend/desktop/           # 桌面网页前端（静态，钉钉风格）
├── weapp/                     # 微信小程序前端
├── scripts/                   # 统一构建/运行脚本
├── data/                      # 运行时数据（自动生成）
│   ├── secret.key             # 自动生成的主密钥
│   ├── users.db               # 账户库（SQLite）
│   └── spaces/{user_id}/      # 每用户加密数据空间
└── dist/                      # 构建产物（desktop / weapp）
```

### 2.2 分层职责图

```
┌─────────────────────────────────────────────────────────────┐
│  客户端：桌面网页（/static）  │  微信小程序（http://127.0.0.1:8600）│
└───────────────┬──────────────────────────┬──────────────────┘
                │  HTTP / SSE               │
┌───────────────▼──────────────────────────▼──────────────────┐
│  routers/（API 层）  auth / chat / kb / profile / reports / vision │
│        │ 依赖注入 get_current_user（解析 Bearer JWT）            │
├───────────────┬───────────────────────────────────────────────┤
│  业务服务层    │  summary · reports · med_cats · prompts         │
├───────────────┼───────────────────────────────────────────────┤
│  能力层        │  llm/（模型适配）   rag/（解析/分块/索引/检索/图谱）│
├───────────────┼───────────────────────────────────────────────┤
│  基础设施层    │  auth（账户+JWT） · crypto（加密） · storage（隔离空间）│
├───────────────┼───────────────────────────────────────────────┤
│  配置层        │  config.py（config.yaml + 密钥自举）            │
└───────────────┴───────────────────────────────────────────────┘
                │
        ┌───────▼────────┐
        │  data/spaces/  │  ← 全部业务数据以 .json.enc 加密落盘
        └────────────────┘
```

---

## 三、核心机制逐条列举

### 机制 1 · 配置与密钥自举
- 由 `config.py` 实现：`_load_yaml()` 读 `config.yaml`；`_ensure_secret(name)` 在 `data/secret.key` 缺失或某项缺失时生成 32 字节随机密钥并写回（幂等）。
- `JWT_SECRET` / `FERNET_KEY` 最终来源优先级：**配置文件显式值 > 自动生成的 secret.key**。

### 机制 2 · 认证与鉴权
- 账户存于 `data/users.db`（SQLite），用户名唯一约束。
- 密码哈希：`crypto.hash_password` 使用 **PBKDF2-SHA256，12 万轮**，随机 salt，绝不存明文（`auth.py` 的 `register/login`）。
- JWT：**自签 HS256**（header.payload.signature），`auth.issue_token` 签发、`auth.parse_token` 校验，有效期 7 天，无第三方依赖。
- 每次请求经 `deps.get_current_user`：校验 `Authorization: Bearer <token>`，失败即 `401`；成功后把 `{id, username}` 注入路由，确保只能访问本人数据空间。

### 机制 3 · 数据隔离与加密（隐私核心）
- `storage.py` 为每位用户建立独立目录 `data/spaces/{user_id}/`，结构：
  ```
  conversations/{conv_id}.json.enc    # 对话
  kb/docs.json.enc / chunks/{doc_id}.json.enc / raw/{doc_id}.json.enc / index.json.enc / graph.json.enc
  profile.json.enc                    # 健康档案
  reminders.json.enc                  # 提醒
  reports/{rid}.json.enc + reports/index.json.enc
  ```
- 加密：`crypto.encrypt_json/decrypt_json` 用 **Fernet**；密钥经 **HKDF-SHA256（salt + info=`user:{user_id}`）从主 FERNET_KEY 派生**，因此不同用户密文互不可解密 —— 实现「硬隔离」。
- 写操作受全局 `threading.Lock` 保护，避免并发写损坏。

### 机制 4 · 大模型适配层与模型切换
- `llm/base.py`：`LLMProvider` 抽象（available / chat / stream_chat / vision / embed_query）；`OpenAICompatibleProvider` 统一覆盖智谱/DeepSeek/OpenAI 等 OpenAI 协议端点。
- 调用 `/chat/completions`（chat/stream/vision）与 `/embeddings`（语义检索），用 `httpx` 异步请求，超时 120s。
- **容错**：上游返回 HTML/非 JSON、账号组未开通、限流等会被翻译成可操作中文报错，而非泛化 500。
- 注册表（`llm/__init__.py`）：`get_provider / list_providers / get_any_provider / default_provider_id`，支持前端随时切换 provider，并在某 provider 不可用时优雅降级到其他可用 provider。

### 机制 5 · RAG 检索（自研 BM25，零外部依赖）
- 解析：`rag/loader.py` 支持 PDF(`pypdf`)/DOCX(`python-docx`)/MD/TXT，全程内存处理不落临时文件。
- 分块：`rag/chunker.py` 提供三种策略 `paragraph`（默认，按段落聚合）/ `fixed`（定长滑窗）/ `sentence`（按句聚合），并给出长度分布、过短/过长占比、相邻重叠等质量统计。
- 索引与检索：`rag/indexer.py` 实现 **BM25**：
  - 中文：单字 + 2-gram（单字 IDF 降权，可命中「发烧↔发热」换词场景）；英文：按词元。
  - **混合检索**：若配置了智谱 embedding，则对 BM25 与余弦相似度做归一化加权（0.5/0.5）。
  - 按 `top_k` 召回，按 `min_score` 过滤低质片段；索引按用户隔离存于 `kb/index.json.enc`。
- 配套治理：`verify.py`（抽样查询算「自身内容可检索率」覆盖率）、`kb_router` 提供 `/diagnose`（逐 token BM25 得分诊断误召回）、`/evaluate`（分块策略对照）。

### 机制 6 · 防幻觉约束
- 系统提示 `SYSTEM_DOCTOR`（`prompts.py`）设「最高优先级红线」：只能依据知识库/用户自述/公知常识，严禁编造药名/剂量/诊断；无依据时必须回复「无法确认，建议就医」。
- 回答须以【来源：文档名-片段N】标注引用；未用知识库则标【依据：用户描述/一般健康常识】。
- 无模型 Key 或模型调用失败时，返回安全兜底文案，绝不凭空作答。

### 机制 7 · SSE 流式输出
- `chat_router.chat_stream` 返回 `StreamingResponse(media_type="text/event-stream")`，逐 token yield 事件：`meta`（引用来源）→ `delta`（增量文本）→ `done`（含自动整理标题）；出错时 yield `error` 后 `done`。
- 前端据此做逐字渲染；后端设置 `Cache-Control: no-cache`、`X-Accel-Buffering: no` 防止代理缓冲。

### 机制 8 · 对话标题自动摘要
- `summary.py`：对话结束后调用 LLM 生成 ≤20 字中文标题（`TITLE_PROMPT`）；任何异常或无模型时回退 `local_title`（取首条用户消息前 18 字），**始终安全降级，不会卡在「思考中」**。

### 机制 9 · 知识图谱（离线、确定性）
- `rag/graph.py`：内置常见病/症词典，在分块文本中「长词优先」匹配实体；同一分块内实体两两连共现边；通过线索词（引发/导致/并发症/伴随/症状）在窗口内判定**有向关系**（如「高血压→引发→脑卒中」）。
- 结果缓存于 `kb/graph.json.enc`，可由 `/api/kb/graph?rebuild=1` 重建；与报告库共用 `med_cats.CATEGORIES` 分层树口径。

### 机制 10 · 报告生成与报告库
- 问诊对话达标后（`chat_router` 的 `generate_report`），用 `REPORT_PROMPT` 让模型产出结构化 JSON（主诉/现病史/初步分析/建议检查/紧急程度等），解析失败则兜底存原文。
- `reports.py` 自动按 `med_cats.categorize(主诉)` 归类、抽取一句话摘要，写入用户专属报告库（支持按分类/关键词过滤、编辑、删除）；同一会话重复生成会更新同一条 `rid` 而非无限堆叠。

### 机制 11 · 拍照 / 视频识药
- `vision_router`：图片 → base64 → 视觉模型（`MEDICINE_PROMPT`，要求结构化药名/功效/禁忌/不可混用药物/风险）；解析出「不可混合使用的药物」「潜在风险」两个关键字段，并判模糊需重拍。
- 视频：用 **opencv（headless）** 均匀抽取最多 4 帧 JPEG（自带 ffmpeg 后端，无需系统 ffmpeg），逐帧识别，取首个清晰帧作主结论，其余并入观察列表。

### 机制 12 · 健康档案与提醒
- `profile_router`：档案（性别/过敏史/慢病/在服药物等）、健康记录增删、基于档案的**行为建议**（有模型调 LLM，无模型返回固定安全建议）、提醒闹钟 CRUD（daily/once）。

### 机制 13 · 双端部署
- 桌面版：`frontend/desktop` 静态页由后端同源托管（`/`、`/static`），布局仿钉钉。
- 小程序版：`weapp/`（WXML 标准 TabBar 排版），默认请求 `http://127.0.0.1:8600`，真机预览需把 `weapp/utils/api.js` 的 `BASE_URL` 改为局域网 IP。
- 构建：`build-desktop` 产出纯静态 `dist/desktop`（可部署 Nginx，反代 `/api` 到后端 8600）；`build-weapp` 校验并打包 `dist/weapp` 供上传审核。

---

## 四、API 路由总览

| 前缀 | 路由文件 | 关键端点 |
|---|---|---|
| `/api/auth` | auth_router | `POST /register` · `POST /login` · `GET /me`（含可用模型列表） |
| `/api` | chat_router | `GET/POST/GET/DELETE /conversations[/{id}]` · `POST /chat/stream`（SSE）· `POST /conversations/{id}/report` · `POST /summarize-all` · `POST /conversations/{id}/summarize` |
| `/api/kb` | kb_router | `GET /documents` · `POST /upload` · `DELETE /documents/{id}` · `POST /search` · `GET /config` · `GET /chunks` · `POST /preview-chunks` · `POST /evaluate` · `GET /diagnose` · `POST /verify` · `GET /graph` · `GET /graph/tree` · `POST /graph/rebuild` |
| `/api` | profile_router | `GET/PUT /profile` · `POST/DELETE /profile/records[/{id}]` · `GET /profile/suggestions` · `GET/POST /reminders` · `PATCH/DELETE /reminders/{rid}` |
| `/api/reports` | reports_router | `GET`（?category=&q=）· `GET /categories` · `GET/{rid}` · `PATCH/{rid}` · `DELETE/{rid}` |
| `/api/vision` | vision_router | `POST /medicine`（图片）· `POST /medicine-video`（视频） |
| `/api/health` | main.py | 健康检查 `{"ok": true, "service": "medical-agent"}` |

---

## 五、一次问诊的完整数据流

```
用户发送消息
  → chat_router.chat_stream
      → deps.get_current_user 校验 JWT，定位 user_id
      → indexer.search(user_id, message)         # 在本用户知识库做 BM25(+语义) 检索
      → 组装 messages = [SYSTEM_DOCTOR + build_rag_context(citations), …历史, 用户消息]
      → prov.stream_chat(messages)               # SSE 逐字流式
      → 每片 delta 以 {type:"delta"} 推给前端
      → 结束：summary.summarize_title 整理标题
      → storage.save_conversation 加密落盘（含 citations 引用）
  → 用户点「生成报告」
      → provider.chat(REPORT_PROMPT) → JSON 解析
      → reports.save_report 归类入库（reports/{rid}.json.enc + 索引）
```

---

## 六、安全与免责

- 密码 PBKDF2-SHA256（12 万轮）；所有业务数据 Fernet 加密落盘，密钥按用户派生，跨用户不可解密。
- JWT 每次请求校验，接口仅能访问本人空间。
- 系统输出仅供健康参考，**不构成医疗诊断意见**；急症请立即就医（代码与提示词中均有强制兜底）。

---

*文档生成依据：`medical-agent` 后端全量源码静态阅读（main / config / auth / crypto / storage / llm / rag / routers / summary / reports / med_cats / prompts）及启动脚本（scripts/）。如后续代码有改动，请以源码为准并同步更新本文件。*
