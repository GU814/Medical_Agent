# ---- Python / 后端 ----
.venv/
__pycache__/
*.pyc
backend.log

# ---- 构建产物 / 调试工具 / IDE ----
dist/
.tools/
.idea/

# ---- 运行时敏感数据（切勿提交）----
data/
# 说明：data/ 含 secret.key（主密钥）、users.db（用户库）、spaces/*.enc（加密用户数据）
#       迁移后端时必须随后端整体带走，但绝不能进入代码仓库。

# ---- 敏感配置（真实 Key 只放本地/环境变量，模板见 *.example.*）----
config.yaml
.env
# 说明：仓库只提交 config.example.yaml / .env.example；
#       本地复制后填入真实 Key，环境变量可覆盖同名配置（见 backend/app/config.py）。

# ---- 迁移清理中已移出的项（备份在上级 _wxp_migration_removed_20260917/）----
frontend/
sample_kb/

# ---- 文档类（按需保留）----
DEPLOYMENT_GUIDE.md
DEPLOYMENT_RESULT.md
WEAPP_VERIFICATION_REPORT.md

# ---- 小程序本地配置（含真实 AppID；模板见 weapp/project.config.example.json）----
weapp/project.config.json
